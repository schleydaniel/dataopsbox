# service.yaml

apiVersion: v1
kind: Service
metadata:
  name: kafka-kraft-svc
  namespace: dataopsbox
spec:
  clusterIP: "10.106.7.84"
  type: LoadBalancer
  selector:
    app: kafka-kraft
  ports:
    - name: "internal"
      port: 9092
      targetPort: 9092